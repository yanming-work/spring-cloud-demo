spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        serviceName: consul-config-service
      config:
        enabled: true
        format: yaml
        prefix: config
        profile-separator: ':'
        data-key: data